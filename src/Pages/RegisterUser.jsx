import { Form, Input, Select, Button } from "antd";
import RegisterUserSection from "../Components/Register/RegisterUserSection";

const RegisterUser = () => {
  return(
    
    <RegisterUserSection/>
    
  )
}

export default RegisterUser;
